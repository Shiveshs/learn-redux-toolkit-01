import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const SimplepostSlice = createSlice({
    name: "posts",
    initialState,
    reducers: {
        addPost: (state, payload) => {}
    }
})
